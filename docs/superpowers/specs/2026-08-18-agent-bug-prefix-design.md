# Agent Bug Prefix Design

## Goal

Add a repository-level response convention to `AGENTS.md`:

> If you identify a bug in a response, prepend that response with `bug identified`.

## Scope

Append one bullet to the existing repository agent rules. Do not modify workspace-wide instructions, application behavior, or any other repository guidance.

## Verification

Confirm the exact instruction appears once in the repository-level `AGENTS.md` and that the Markdown remains valid.
