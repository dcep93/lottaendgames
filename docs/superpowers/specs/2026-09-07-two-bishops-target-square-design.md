# Two Bishops target square

Continue the existing Find loop in lottaendgames workflow in this task. Implement understood fixes without another design checkpoint, run focused checks, then load the first non-mating witness at cursor 0, preferably a four-ply loop. Preserve existing uncommitted policy work.

Replace r17 with r10, immediately after r8 and before r11: “Prefer fewer diagonals for Black's king, then White king's step proximity to the target square, then bishops off the target corner's edge, except the Phase 2 diagonals.”

Extend r3 (2026-09-08) in its existing ordered place: “Prefer White's king out of the corner, then bishops not adjacent to a cornered White king, then bishops out of the corner.” Compare resulting positions lexicographically by White-king corner occupancy, the number of bishops one king step from a cornered White king (zero when White is not cornered), and the number of bishops on any of a1/a8/h1/h8. Higher rules retain precedence. Verify each priority and all board symmetries, then freshly verify both previous stalemate roots under the changed policy; historical census results do not certify this revision. The initial r3-only recheck compared Kc2 from `8/8/8/3k4/8/8/4B3/2K3B1 w - - 20 11` without changing r10; the subsequent escape correction below supersedes that comparison.

For each resulting eligible adjacent-diagonal wall, target candidates are all outer-wall squares minimizing king-step (Chebyshev) distance to Black's king, including every tie. White's location does not affect this candidate set. If any candidate is occupied by a bishop, the entire set for that wall is invalid. Adjacency to Black is not required; never substitute a farther square when a closest candidate is bishop-occupied.

White's king must be strictly outside the wall, opposite Black across its outer diagonal, or on the outer diagonal and no farther from a candidate target than Black is, using king-step distance. Apply this eligibility condition after choosing the closest candidate set. A king inside the wall cannot create a target. For White on the outer wall, compare distances separately for each closest candidate; equal distances qualify. Occupying a target remains eligible since White’s distance is zero. For target discovery, Black may occupy the inner diagonal. Preserve the minimum diagonal length, four-diagonal enclosure floor, and other rules' enclosure requirements.

Compare complete wall profiles in the requested order: Black's diagonal count, then the existing target-corner edge penalty with its Phase 2 exception, then White's steps to a target. Remove the intermediate Black-to-inner-wall distance preference. Apply the same order both when selecting a wall profile and comparing White moves. Target existence is no longer a separate preference; a smaller enclosure wins even without a target. An inner wall Black can enter on the next move receives the no-enclosure score. The 2026-09-08 escape correction also gives no enclosure credit if any legal Black reply crosses beyond both walls to the opposite side, whether that side is smaller or larger. This replaces the previous maximum-of-separate-sides count. Thus Bf3+ allowing Kc4 receives no enclosure credit. Bd8+ allowing Kf4 loses credit for that orientation; its other orientation still counts ten diagonals. Preserve target and beyond-wall geometry independently of count eligibility. For a qualifying enclosure, count Black's original side, plus any reachable outer-wall diagonal and any inner diagonal screened by White's king. White must stand on that inner diagonal at least two squares from the nearest board edge to count as a screen; a king on the edge or one square from it does not. Add each wall diagonal once. A reply onto the outer wall remains distinct from crossing beyond it; Ke6 allowing Kg8 in the h1-wall example still counts eight, including that outer-wall diagonal. Do not introduce a flood fill or change actual legal moves.

Add the r4 retreat from `4Bk1B/8/5K2/8/8/8/8/8 w - - 0 1`: recognize all four Be8 retreats to d7/c6/b5/a4, preserving White Kf6 and Bh8, under every rotation/reflection. Subsequent rules may choose among those equally credited r4 moves. Verify all four pattern matches, rejection of other geometry, and all policy continuations from both previous stalemate roots. Recheck the user’s second start `8/8/8/3k4/8/8/4B3/2K3B1 w - - 20 11`: Bf3+ must lose its enclosure count because of Kc4, while Kc2 retains eight diagonals. Record the actual selected move and its deciding rule. Run focused policy tests and TypeScript checks; retain historical census and r3-only reports as dated results, and save a fresh recheck report for this revision.

Move the beyond-wall preference from r18 to r12, immediately after r11 and before r19: “Without a target square, prefer White’s king closer to the diagonal one beyond the outer wall.” It applies only to positions with no valid target but an eligible wall. Measure White's king-step distance to the parallel diagonal one index beyond the outer wall, away from Black's target corner. Use the best resulting wall profiles retained by r10; retain tied profiles and use their minimum beyond-diagonal distance. A usable target, or absence of any eligible wall, leaves r12 inactive.

For `8/7k/8/5K2/8/8/BB6/8 w - - 0 1`, the target corner is h1, outer wall a2–g8, and beyond diagonal a3–f8. Bb3 and Ke6 both have no usable target. Bb3 counts seven diagonals; Ke6 counts eight including the reachable wall diagonal. R12 distances are two and one, respectively, but r10 takes precedence; Bf6 is now selected: Bb1 gains the reachable screened inner diagonal and counts eight, while Bf6 counts seven.

The target definition and diagram precede the Phase 2 note. Use White Ke4, Black Kg3, bishops d1/d2, the outer diagonal c1–h6, and target f4; the arrow from Black to f4 illustrates closest-square selection. Optional zero-based noteIndex associates a diagram with its note without moving unrelated diagrams.

From `3k4/8/8/4B3/5K2/7B/8/8 w - - 0 1`, Ke4 must be uniquely preferred with target c7. Bg4 leaves White on f4, which is not a closest candidate, and therefore has no target. Tests also cover adjacent candidates e3/f4 with Black f3, invalidation when only e3 is bishop-occupied, outside/on-target eligibility, checking escapes, smaller-side crossings, rotations/reflections, all three r10 subpriorities, and r12 activation and ordering.

For `8/8/7k/8/8/4K3/2BB4/8 w - - 0 1`, r10 prefers Bc3 (six diagonals) over Ke4 and Kd4+ (ten diagonals each). Their checking wall permits Black to cross beyond both walls, so only their other orientation receives enclosure credit. That orientation has no target and gives Ke4 a beyond-wall distance of three, versus two for Bc3.

For `8/6k1/3K4/8/8/7B/7B/8 w - - 0 1`, Ke5 reaches the closest target e5, two Black king steps away, and wins the r10 comparison. Ke6 has no target because White is inside the wall and counts six including a reachable screened inner diagonal. Bg3 now wins overall under r10’s edge subpriority by moving the bishop off the corner edge before r10 compares targets.

After `1. Bc3 Kh5` from `8/8/7k/8/8/4K3/2BB4/8 w - - 0 1`, Bd2 allows Kh6 on the screened inner diagonal, invalidating that orientation; its other eligible orientation counts ten diagonals, losing to Kd4’s six.

R6 only credits the Phase 2 diagonals if every legal Black reply remains strictly behind the wall. A reply onto either wall diagonal invalidates confinement. The Black-facing inner wall must also have an uninterrupted bishop ray across its full extent, even when Black cannot enter the screened tail on its immediate reply, except that White’s king one square from the board edge is ignored as a ray blocker for this screening check. Keep actual attacks and legal Black replies unchanged. In the Phase 2 template this is the shorter `outerDiagonal`; preserve that template naming. Checking both endpoints detects an intervening king without rejecting a king at an endpoint, where no ray continues beyond it. Screening the farther wall alone does not invalidate the Black-facing barrier. In `8/8/8/8/7k/5K2/2BB4/8 w - - 0 1`, Bd1 allows Kh5 on the screened inner diagonal and must receive no Phase 2 diagonal credit. Keep the inclusive Black-area geometry used for White king path restrictions unchanged.

Add the loaded r4 mating pattern from `8/8/8/k1B5/2BK4/8/8/8 w - - 0 1`: `1. Kc3 Ka4 2. Bb6 Ka3 3. Bb5 Ka2 4. Kc2 Ka3 5. Bc5+ Ka2 6. Bc4+ Ka1 7. Bd4#`. Match the exact piece geometry at each stage under all rotations/reflections. After `4...Ka1`, use `5. Bc5 Ka2` to rejoin the same finish.

Add the exact r4 entry `1. Be7` from `8/2k5/2B5/1KB5/8/8/8/8 w - - 0 1`, under all rotations/reflections. It moves the c5 bishop while preserving the c6 bishop and b5 king; Black may reply Kb8 or Kc8. After either reply, add `2. Kb6` to r4 with the bishops on c6/e7 and White king on b5, under all rotations/reflections.

From `8/8/BB6/8/1K6/8/8/3k4 w - - 0 1`, Ba5 allows Kd2 on the inner wall and gets no diagonal count, while Kc3 keeps the inner wall unreachable and counts five diagonals. Kc5 also counts five and wins the later r12 distance preference.

R22 remains after r19 and before r25. When White's king is on or inside the inner wall of the smallest adjacent-diagonal enclosure, select non-edge inner-wall squares closest to White in straight-line distance, excluding White's occupied square and any candidate closer to Black than to White. Keep exact ties. First place the inner bishop there while retaining the outer bishop on its wall diagonal. Once the inner bishop is placed, the outer bishop may slide along that same wall while White’s king approaches or holds a behind-bishop square; do not require the outer bishop’s original square. Then minimize White's king-step distance to an adjacent square strictly inside Black's area and farther from Black in straight-line distance than the bishop is. Retain all such king destinations. Use starting-position geometry consistently across candidate moves. No eligible wall or candidate, or White outside, leaves r22 inactive. The revised example selects Be5 from `2k2K2/8/8/8/8/6B1/6B1/8 w - - 0 1`; Bd6 is closer to Black in straight-line distance and is excluded. With Be5 and Black Ke7, f5 is a behind-bishop destination; d4 outside the wall is not. This supersedes the earlier edge endpoint and outside-wall definitions.

R4 recognizes corner preparation with White already on the Phase 2 king square and a bishop controlling the third edge square (f7/h6 for h8). A preparation must preserve that control, force Black to the adjacent edge square, and leave a verified mate in two. This admits `Ba4 Kh7 Bc2+ Kh8 Bg7#` from `4BB1k/5K2/8/8/8/8/8/8 w - - 0 1`, and also the preparation `Bd7`, under rotation/reflection. Existing pattern matches retain precedence.

Verification: focused policy suite, relevant guide ordering checks, and TypeScript compilation; then stop at the first usable non-mating witness. No full visual pass or exhaustive content suite is needed.

Self-review: closest-square selection supersedes the previous adjacency requirement. All requested behavior is specified, wall scores remain coherent, and unrelated book content is unaffected.

For `8/8/6B1/4K3/8/4B3/8/3k4 w - - 2 2`, r10 now prefers Kd4 over Bd3: both retain five diagonals, but Kd4 puts White two steps from target c2. Black's distance from the inner wall must not affect either wall selection or move comparison.


Add r11 between r10 and r12: “Play the flank step.” It requires orthogonally adjacent bishops straddling ranks 4–5 on a shared file, or files d–e on a shared rank; White’s king adjacent to both; and the kings aligned perpendicular to the bishop line, exactly three king steps apart, on opposite sides of that line. Evaluate these conditions before White moves. Extend the bishop line one square past the bishop level with White’s king, away from the other bishop, and prefer the legal king move there. In `8/8/8/2B5/k1BK4/8/8/8 w - - 0 1`, this is Kc3. R11 resolves candidates retained by r10; with target proximity restored in r10, Kc3 can already win there. Preserve all eight rotations/reflections and translated placements that satisfy the central-half condition. Otherwise r11 is inactive. Validate the positive pattern, each missing precondition, rule ordering, and the next non-mating witness.


Restore closest outer-wall targets without the Black-adjacency restriction. Keep all tied minimum king-step candidates before occupancy and White-king eligibility checks. In `8/4k3/8/3BB3/3K4/8/8/8 w - - 0 1`, c6 and d5 tie as closest squares on a8–h1. Since d5 is bishop-occupied, that wall still has no target under the existing occupied-candidate rule. Verify distant candidates and ties under all board symmetries, update notes and diagram captions, and retain r11 and r10 ordering.


Combine r9 and r10 into r10, removing the standalone r9. Compare diagonal count first, then the existing bishop corner-edge penalty with its Phase 2 exception, then White’s target distance. Preserve the existing corner-selection geometry for the edge score; this is an ordering change, not a new wall definition. Rename the score and guide diagram consistently.

For `8/1k6/4K3/8/8/B7/B7/8 w - - 0 1`, r10 selects Bb3 instead of Kd5: equal diagonal counts leave edge penalties zero versus one to decide before Kd5’s better target distance.

For `8/4k3/6K1/8/8/8/BB6/8 w - - 2 2`, combined r10 selects Ba3+ with five diagonals and one edge penalty over Bb3 with six diagonals and zero edge penalty.

For `8/6B1/8/8/5K1k/8/2B5/8 w - - 2 2`, Bh6 leaves White Kf4 screening h6 from c1 on the inner wall. Black can reach c1 through h3, g2, f1, e1, d2, c1 with White fixed, so Bh6 counts six rather than five diagonals. The existing target and edge subpriorities still apply after the corrected count.

The Bh6 count fix alone tied r10 on six and left r12 favoring Bh6. The added outer-wall eligibility below now lets Ke5 reach toward f6 under r10 and break that loop.

For `8/6B1/8/8/5K1k/8/2B5/8 w - - 2 2`, Ke5 leaves White on outer wall a1–h8, one step from target f6 versus Black’s two. This now qualifies f6 and lets r10 prefer Ke5 over Bh6. Test equality as eligible and greater White distance as rejected, and preserve bishop-occupied candidate invalidation before this distance comparison.


Add the r4 retreat `2. Bc1` with White Kc3, Black Ka2, the moving bishop on b2, and the other bishop anywhere on c2–h7 (c2, d3, e4, f5, g6, or h7). Preserve that other bishop and the king; Bc1 forces ...Ka1. Recognize every rotation/reflection, and reject missing king or diagonal geometry.


For `8/8/6B1/8/8/5K1k/3B4/8 w - - 0 1`, Bh5 receives no r6 diagonal credit because White Kf3 screens its d1–h5 wall. This does not depend on an immediate Black escape onto that diagonal. Test all eight symmetries and verify that clearing a screened wall restores credit before bishop-square preferences.


For `8/8/6B1/8/8/5K1k/3B4/8 w - - 0 1`, Bh5 counts five diagonals because White Kf3 screens d1–h5, even though Black cannot reach its hidden tail. Ke4 also counts five and has target f5 one king step away, so r10 uniquely prefers Ke4. The guarded-tail test with White Kd2 and Bh6 counts five under the one-square edge exemption. Verify both cases and the endpoint exception under all eight symmetries.


Exempt White’s king from screening in both r6 and r10 when its minimum distance to a board edge is one square. In `8/8/8/8/1k6/3BB3/3K4/8 w - - 0 1`, Ke2 now retains five Black diagonals, tying Kc2; r12 prefers Ke2 because its beyond-wall distance is one rather than two. Kings at least two squares from the edge, such as f3 screening Bh5, still count as screens. Test all eight symmetries, retain the endpoint exception, and preserve rejection when an actual legal Black reply reaches a wall.


Extend r22 to allow the outer bishop to make room along its existing wall, while retaining the placed inner bishop and scoring the king’s distance to the same starting-position behind squares. From `8/8/8/8/8/3k4/BB6/1K6 w - - 2 2`, Bf7 preserves the b2 inner bishop and holds White Kb1 behind it, so it must beat the retreat Kc1 under r22. Bg8 satisfies the same r22 conditions and wins overall under the existing r30 bishop-distance preference. Moving the outer bishop off its wall, or moving the placed inner bishop away, does not get placement credit. Preserve all eight symmetries and existing inner-bishop-first behavior.


Add r18 between r12 and r19: ‘Play the choke move.’ For the smallest existing adjacent-diagonal wall profiles, if the inner wall has exactly five squares and none touches either edge incident to its target corner, prefer the inner-wall bishop on the board’s long diagonal. Determine eligibility and the target from the starting position, then score whether the result retains or reaches that square. In `4B3/6K1/4k3/8/7B/8/8/8 w - - 0 1`, the inner wall is d8–h4, target corner a1, and long diagonal a1–h8, so prefer h4–f6. Preserve all rotations/reflections. Other wall lengths, walls touching a target-corner edge, and absent walls leave r18 inactive. Add a native guide diagram with the exact pieces, five-square wall, target corner, long diagonal, and h4–f6 move arrow. Keep the earlier target-square note and diagram ordering. Verify rule order, positive and negative geometry, retained placement, diagram rendering, TypeScript, and the next non-mating witness.


Exhaustive audit request (2026-09-08): supersedes the first-witness stopping rule
for this run. Enumerate the entire app-eligible Standard KBBK universe and every
reachable selected-policy continuation, with no root/node cutoff. Report at most
five playable non-mating witnesses, or certify all positions terminate. Count
moves first eliminated by every ordered rule and positions affected, once per
D4-canonical White state. Include universal 100-ply draw checks. Fix measured
semantics-preserving inefficiencies and verify unchanged move scores, choices,
and census counts. Keep a resumable fingerprinted checkpoint, parallelize scoring,
and continue the census after discovering failures. Report remaining performance
or rule-interpretation problems without silently changing policy during the run.


Allow equal target distance (2026-09-08): the outer-wall condition is now White distance <= Black distance, using king steps. Preserve the outside-wall exception, reject White inside, and invalidate every target if any closest candidate is bishop-occupied. Check candidates individually when distances differ. From `5BB1/8/8/1K6/8/8/1k6/8 w - - 24 13`, Kb4 now qualifies target a3 at one step, tying Ka4 under r10; r25 prefers Kb4. Cover nearer/equal/farther cases, mixed tied candidates, all eight board symmetries, and updated guide wording. Recheck the prior long mating lines under the changed policy without replacing historical reports.

King-wall correction (2026-09-08): revert the global `king wall` guard introduced in e1a9cde, including its changed move expectations and recheck report. The one-square-from-edge condition belongs in determining what counts as a bishop wall, not in a global king-move restriction. This undo restores the preceding wall calculations and rule order; it does not introduce replacement wall-scoring behavior. Preserve the equal-distance target change above.

R10 outer-wall screening (2026-09-08): implement the clarified enclosure criterion. If White's king screens a profile's outer diagonal at least two squares from the nearest board edge, that profile receives the no-enclosure diagonal score, even when Black has no immediate crossing reply. A king one square from the edge retains the existing screening exception; an endpoint has no hidden ray beyond it and is not a screen. A screened inner diagonal still belongs to Black's count while an intact outer wall bounds the area. Evaluate resulting positions for both bishop and king moves, retaining independently valid orientations and existing target/beyond-wall geometry. Do not add a global move restriction or change other rules' priority. From `5B2/3k4/8/8/4K3/1B6/8/8 w - - 18 10`, Kd5 screens outer wall a2–g8 and gets no enclosure credit; Ke5 retains five diagonals and wins under r10. Verify all eight symmetries, edge exceptions, stationary-king bishop moves, unchanged inner-screen counting, and Phase 2 king movement. Recheck the preceding loop and record the next non-mating witness without rerunning the full census.

Both-wall qualification correction (2026-09-08): supersede the preceding screened-inner counting and retained screened-profile geometry. For r10/r12, a pair exists only when White's king screens neither diagonal. Reject a centrally screened inner or outer diagonal before computing enclosure credit, target squares, or the one-beyond-wall distance. Preserve the one-square-from-edge exemption and endpoint non-screening, independently valid orientations, and geometry for profiles invalidated only by an actual immediate Black escape. Do not add a global move guard, change the existing target-corner edge subscore, or alter established r4/r6/r18/r22 template rules. After Bh5 from `8/8/6B1/8/8/5K1k/3B4/8 w - - 0 1`, Kf3 screens inner d1–h5, so c1–h6 does not supply an outer-wall target or r12 distance. Ke4 also screens the original outer b1–h7 and must not retain that profile. Check both cases, all eight symmetries, edge exceptions, stationary-king bishop moves, valid alternative orientations, and the next non-mating witness. Keep previous reports as historical results.

R10 target-before-edge priority (2026-09-08): the current order is fewer Black diagonals, then White king-step distance to a target, then the existing bishop corner-edge penalty with its Phase 2 exception. This supersedes all earlier edge-before-target ordering in this living spec. Change the ordered comparator and displayed wording without changing wall qualification, target discovery, or corner-edge scoring. From `8/8/4K3/8/8/4k3/1B6/1B6 w - - 42 22`, Kd5 and Bg6 each retain six diagonals and target d4; Kd5 is one step from d4 versus two for Bg6, so Kd5 now wins despite its bishop remaining on the corner edge. Verify the tradeoff under all eight symmetries, diagonal count taking precedence over target distance, and the edge penalty deciding only after both earlier scores tie. Recheck the previous draw line and loaded position, recording current proof bounds or the next non-mating witness without rerunning the census.

Controlled-screen correction (2026-09-08): replace the blanket one-square-from-edge exception with actual White control of the screened ray. On each bishop wall diagonal, enumerate every square beyond White's king away from that bishop. A wall is invalid if any such square is not attacked by White in the resulting position. A controlled hidden tail is valid; an endpoint has no hidden tail. Use the same criterion for both r10/r12 wall diagonals and r6's Black-facing Phase 2 barrier, preserving r6's other endpoint-blocker and legal-reply checks. Reuse the existing Chess position for attack checks; do not change legal moves or add a global king-move guard. Invalid r10 pairs supply no count, target, or beyond-wall distance; independently valid orientations remain available. With Bh7 and Kg6, hidden f5 is king-controlled but e4, d3, c2, and b1 are uncontrolled, so the b1–h7 wall is invalid despite the king's distance of one from the edge. With Bh7 and Kc2, only b1 is hidden and Kc2 controls it, so that wall is valid. Verify both directions under all eight symmetries, inner and outer walls, every hidden square rather than only the nearest, stationary-king bishop moves, endpoints, and r6 agreement. Update guide wording and recheck current witnesses with fresh proof maps without rerunning the census.

R18 outside-king condition (2026-09-08): add the user's condition to the existing choke rule: White's king must be strictly outside the wall, opposite Black beyond the outer diagonal, in the starting position. White on either wall or inside does not activate r18. Keep the existing smallest-wall selection, five-square inner diagonal, target-corner edge exclusions, long-diagonal targets, starting-position geometry, and rule ordering. Update the displayed rule and the existing diagram caption to state the outside condition; its Kg7 example remains valid. Verify the reported Bc6/Kd6/Ba4/Ke6 loop loses r18 eligibility, positive and boundary cases under all eight symmetries, focused policy/guide tests, and TypeScript. Recheck the loop's full reachable graph and find the next current non-mating witness from clock zero using fresh evaluations.

Workflow update (2026-09-08): the user now requires no commits or pushes in this task unless explicitly requested. After changing rules, finish focused validation and stop the search at the first verified non-mating starting position. Return its replay link; do not continue additional root proofs or a census after finding it.

R10 minimum target distance (2026-09-08): score White's target proximity as at least one king step, so occupying a target ties being one step away. Apply this floor before comparing wall profiles as well as moves. Preserve actual distances for choosing closest outer-wall candidates and deciding White's eligibility; no target remains 99, and other rules' distances remain unchanged. From `3k4/8/5K2/8/8/B7/B7/8 w - - 56 29`, Kf7 and Bb3 now tie on five diagonals and a proximity score of one, so Bb3's better corner-edge score beats Kf7. Update the displayed r10 wording, verify symmetric cases and distances above one, then return the first verified current non-mating starting point without committing or pushing.

R24 resulting-wall correction (2026-09-08): supersede the starting-wall eligibility below. Keep the starting king moat fixed, but use each candidate's resulting r10 enclosure status for the no-wall condition. A finite diagonal count disables r24 and gives zero penalty, whether that move creates, repairs, or retains the wall; no target alone still does not qualify. Reuse the r10 score already computed rather than generating hypothetical Black replies from the starting position again. From `8/4k3/2K1B3/8/8/6B1/8/8 w - - 2 2`, Bg4 and Bd7 both give five diagonals with target d6, so r24 must reject neither. Bg4 survives r24; the existing later bishop-distance preference selects Bh3 overall. Verify this under all symmetries, gate changes when walls are created or broken, the original Be6+ example, and stop at one current non-mating witness without committing or pushing.

R24 king-moat bishop (2026-09-08): insert r24 between r22 and r25 with the user's exact text, “If there is no bishop wall, prefer a bishop inside a king moat.” When the kings are two king steps apart, a king moat is the midpoint rank or file on an axis where their coordinates differ by exactly two, following the latest established moat helper (opposition, knight separation, or two-square diagonal separation). Retain both lines if both axes qualify. Fix the moat and no-wall eligibility from the starting position. A bishop wall uses r10's adjacent-diagonal geometry, full screened-tail control, and immediate Black escape checks; lack of a target alone does not remove a wall. When there is no valid wall and at least one moat, prefer resulting positions with at least one bishop occupying any moat line; do not add a preference for a second bishop. Otherwise r24 is inactive. Existing higher rules retain precedence. In `8/3k4/8/4K3/8/8/B6B/8 w - - 0 1`, the sixth rank is the moat and Be6+ must beat the king shuffle. Add a guide definition and focused symmetric tests, run the policy/guide and TypeScript checks, and return the first current clock-zero non-mating witness without committing or pushing.


R9 outer-wall placement (2026-09-08): insert r9 between r8 and r10 with the exact text, “If White's king is inside the smallest bishop wall, place it on the outer wall.” Fix the wall geometry from the starting position. Qualify wall pairs using r10's length/count bounds, controlled screened tails, and legal Black steps: a reachable inner wall or escape beyond both walls invalidates the enclosure. Compare all valid profiles by their Black diagonal count before checking White's location; retain ties. White is inside when strictly on Black's side of the outer wall, including the inner wall. Prefer legal king moves directly onto an eligible smallest outer wall; if none exists, leave r9 inactive rather than adding a distance preference. Bishop moves do not receive placement credit. Compute starting Black replies once per scoring context after cheap geometry and direct-reachability checks. Do not require the resulting wall to remain valid, since the requested Kc5 screens part of its original wall. From `1k6/8/2BK4/8/8/8/8/6B1 w - - 0 1`, inner a8–h1 and outer a7–g1 enclose seven Black diagonals; Kc5 is uniquely selected by r9 even though its resulting r10 count is 99. Verify ordering, direct placement, starting eligibility, smallest-wall selection, boundary and screen cases, board symmetries, policy regressions, guide wording, and TypeScript, then stop at one verified fresh-clock non-mating witness. No commits or pushes.


R22 removal and r10 screen simplification (2026-09-08): remove r22 from the ordered policy and guide, its scoring fields, starting-context plan, and obsolete tests. In r10, ignore king screening of the outer wall. Reject an inner-wall king screen whenever any square exists beyond the king away from its bishop, regardless of control or distance to the edge. An endpoint has no hidden ray and is not a screen. Preserve legal Black reply checks, closest-target selection and eligibility, the minimum-one proximity score, and independent wall orientations. The new r10 score naturally supplies r12 and r24; scope the change to r10 as requested, preserving r9's starting-position controlled-screen eligibility and r6's Phase 2 screen test. Use a constant-time geometric check instead of enumerating and checking attacks on a hidden tail for r10, and remove its unused Chess parameter. In `8/8/8/2B5/8/1k1B4/4K3/8 w - - 2 2`, both Ke3 and Kf2 satisfy r9 and retain five diagonals under r10; Ke3 now wins through r25. Update guide notes, screen regressions and board symmetries; preserve actual escape and mating tests. Stop after one verified fresh-clock non-mating witness. No commits or pushes.

Bishop-occupied target candidates (2026-09-08): remove the rule that invalidates the target set when any closest outer-wall candidate is occupied by a bishop. Retain every closest candidate, including occupied squares, subject to the existing White king eligibility condition. Preserve closest-to-Black king-step geometry, the scored minimum-one distance, screen qualification, actual Black escape checks, and ordered rule priorities. Update the target note and diagram caption. In `8/8/6B1/8/4K2k/8/3B4/8 w - - 2 2`, Kf5 should retain f5 and g6 as targets despite Bg6, with the same proximity score of one as Bh7; verify the resulting policy comparison under all eight symmetries. Keep the mate-before-draw regression active and stop the search at one verified fresh-clock non-mating starting position. No commits or pushes.

Shared smallest-wall preference (2026-09-09): for every rule that chooses bishop walls, first retain the smallest eligible walls by that rule's diagonal count. Among those ties, prefer walls where White's king is not inside: strictly outside or on the outer diagonal. If every tied smallest wall contains White, retain all of them. Never choose a larger wall because of White's location, and do not prefer strictly outside over on the outer wall. Use one shared selector, preserving each rule's existing wall qualification and starting/result position timing. Apply it to r6's template corners (also used by r5), r9's starting qualified walls, r10's result wall profiles before target-distance comparison (inherited by r12/r24), r18's starting choke walls, r19's result outer-bishop identity, the r10 corner-edge targets, and the public wall-corner helper when White's square is supplied. R19 now uses the selected smallest walls instead of any wall; corner-edge selection chooses size before White's side instead of its previous outside-first ordering. Keep exact mating templates, king eligibility, target occupancy, distance floor, screen rules, and actual Black escape checks unchanged. In `8/8/8/5B1k/5B2/5K2/8/8 w - - 0 1`, the h8-side wall ties the h1-side wall at five diagonals and leaves White outside, so r9 becomes inactive. Verify the Bc7 comparison and all board symmetries, outer-wall boundaries, all-inside ties, smaller-inside versus larger-outside walls, shared rule behavior, guide text, and TypeScript. Keep the existing mate-before-draw regression active; stop at one current fresh-clock non-mating witness, load it at cursor 0 in the only tab, and do not commit or push.

R9 outer-wall proximity and r12 removal (2026-09-09): remove r12 from the ordered rules and guide, its score field, and its unused one-beyond-wall distance calculation. Replace r9's help with “If White's king is inside the smallest bishop wall, prefer proximity to the outer wall.” Keep r9's starting-position qualified wall selection, including controlled-screen and actual Black reply checks and the shared smallest-count/White-not-inside tie preference. Remove the direct-arrival precondition and retain every square on each selected outer diagonal as the fixed distance destinations for this turn. Compare the resulting White king's straight-line distance to its closest destination, using squared Euclidean distance without a square root. Distance zero is best; r10's separate minimum-one king-step score stays unchanged. Bishop moves cannot redefine the starting target wall and leave r9's king distance unchanged. Missing walls or White outside/on a preferred starting wall leave r9 inactive. The requested `1. Ke7` from `3K1B2/5B2/2k5/8/8/8/8/8 w - - 0 1` resolves the otherwise ambiguous proximity metric: Ke7 is one straight-line unit from outer a2–g8, while Ke8 is sqrt(2) and the original Kd8 is sqrt(5); king-step distance would tie Ke7/Ke8 and cause r10 to choose Ke8. Verify Ke7 wins, all symmetries, graded approach and zero-distance arrival, fixed starting walls, tie selection, higher-rule precedence, removed r12 behavior, guide text, and TypeScript. Keep the mate-before-draw regression active and stop at one current fresh-clock non-mating witness. No commits or pushes.

R9 resulting-position measurement (2026-09-09): supersede r9's fixed starting wall. Select and qualify walls from each position after White's candidate move, including resulting bishop placement, White's location, controlled hidden rays, and legal Black replies. Then apply the shared smallest-count/not-inside tie preference, activate r9 only if White is inside the selected resulting wall, and use straight-line proximity to its outer diagonal. Outside/on-outer/no-qualified-wall results remain nonapplicable under the existing selector semantics, not artificially ranked against inside results. Remove r9 from the starting context and reuse the moved Chess instance, adjacent wall list, and already-generated Black replies; no cloned board or extra move generation is needed. R10's distinct inner-screen criterion and minimum-one king-step distance remain unchanged. From `8/8/3B4/3BK3/7k/8/8/8 w - - 2 2`, Kd4 repairs inner b8–h2 with outer a8–h1, leaving White outside the six-diagonal wall; r9 becomes inactive for Kd4 and r10 selects it over Ke6. Verify repairs, broken/created/moved bishop walls, resulting inside/outside/outer-wall boundaries, actual Black escapes, shared ties, metric, unchanged conditional-rule behavior, board symmetries, guide text, and TypeScript. Retain the active mate-before-draw regression; stop at the first fresh-clock non-mating witness, load its replay at the beginning in the only tab, and do not commit or push.

R10 outer-bishop safety before target proximity (2026-09-09): use the exact rule text, “Prefer fewer diagonals for Black's king, then outer bishop at least 3 steps from Black's king but off target corner's edge, then White king's step proximity to the target square.” The middle preference is one combined condition: the same resulting outer bishop must be at least three king steps from Black and off both board edges incident to its wall's target corner. There is no partial credit or additional reward beyond three steps. Only the outer bishop is tested; remove the old inner-bishop edge penalty and the Phase 2 diagonal exemption. Preserve the target-distance floor of one, so occupying a target and standing one king step away tie. Compare complete resulting wall profiles by diagonal count, this combined outer-bishop penalty, then target distance, after the shared smallest-wall/not-inside tie selection. Never combine an outer bishop from one wall with another wall's target score, and give invalid count-99 walls no outer-bishop credit. Keep r19's existing distance-only fallback and earlier Phase 2 rules; removing r10's exemption does not prohibit edge moves globally. Replace the r10 guide diagram with White Ke5, Black Kg3, outer Bh6, inner Bd1, target h1, and arrow h6–d2: Bh6 is three steps away but on the edge; Bd2 is three steps away and off the edge; the inner Bd1 edge is ignored. Validate the threshold, both conditions, Phase 2 exception removal, inner-bishop exclusion, profile coherence, priority order, existing screen/target semantics, all eight symmetries, guide rendering, and TypeScript. Preserve the active universal mating regression; stop the search at the first verified fresh-clock non-mating witness and load it at cursor zero in the only tab. No commits or pushes.


R10 outer-bishop edge preference only (2026-09-09): replace the rule with “Prefer fewer diagonals for Black's king, then outer bishop off target corner's edge, then White king's step proximity to the target square.” Remove the three-king-step condition from r10 only. The outer bishop receives edge credit regardless of its distance from Black; the inner bishop remains excluded and Phase 2 diagonals still give no edge exemption. Preserve complete resulting wall profiles, count-first ordering, shared smallest-wall ties, screening and reply qualification, occupied targets, and the minimum-one target distance. R19 remains the later distance preference, but cannot override a distinction already made by r10. From `8/8/7B/8/4K3/8/2B5/4k3 w - - 2 2`, Bb1 has outer-edge penalty one while Kd3 leaves Bc2 off the h1 corner edges with penalty zero, so r10 rejects Bb1 and Kd3 is uniquely selected. Update the diagram to h6–g5 with White Ke5, Black Kg3, inner Bd1: Bg5 earns edge credit despite being only two king steps from Black. Verify the example and board symmetries, distance independence, retained exceptions and later-rule behavior, focused policy/guide tests, and TypeScript. Keep the universal mating regression active, stop at one verified fresh-clock non-mating starting point, load its replay at cursor zero in the only tab, and do not commit or push.


R10 Euclidean target proximity (2026-09-09): replace White's king-step target score with Euclidean proximity, preserving the current count-first, outer-bishop-off-edge-second ordering. Compare squared Euclidean distances to avoid square roots; this preserves the requested ordering exactly. Keep the score floor at one, so occupying the target and orthogonal adjacency tie, while diagonal adjacency scores two internally (actual distance sqrt(2)). Keep the missing-target sentinel 99, which exceeds the largest squared board distance of 98. Change only White's proximity score: selecting the closest outer-wall candidates to Black and testing on-wall White eligibility still use raw king-step distance. Preserve coherent resulting wall selection, screen and legal-reply qualification, tied-wall preferences, occupied candidates, and all other rules. From `8/8/5KB1/8/8/5k2/8/2B5 w - - 26 14`, Ke5 and Kf5 retain five diagonals, an off-edge outer bishop, and target e4; Ke5 scores one while Kf5 scores two, so r10 now selects Ke5 before r25. Update the rule text and notes; verify this fixture across all symmetries, the Euclidean floor and diagonal distinction, unmodified target discovery/eligibility, profile selection, ordered priorities, policy/guide tests, and TypeScript. Keep the universal mating regression active. Stop at the first verified fresh-clock non-mating witness, load it at cursor zero in the only tab, and do not commit or push.


Outer-wall screens allowed in r9 (2026-09-09): White's king screening the outer bishop must no longer disqualify a wall. R10 already allows these screens; change r9 to apply its controlled-screen test only to the inner bishop. Preserve r9's existing inner-screen requirement that every hidden square be controlled, r10's stricter inner-screen criterion, and all actual Black-reply confinement checks. Phase 2's existing screen check applies to the Black-facing inner wall (historically named outerDiagonal in its template), so it does not need relaxation. Keep resulting-position measurement, smallest-wall/not-inside selection, Euclidean proximity, and all ordered rules unchanged. From `3k4/8/2K5/8/1B6/1B6/8/8 w - - 2 2`, Kd5 screens Bb3 on outer a2–g8, leaving f7/g8 uncontrolled; this is now allowed. R9 keeps that five-diagonal enclosure with inner a3–f8, sees White on the outer wall, and is inactive. R10 retains targets e6/f7 and selects Kd5, breaking the Bb4/Kd8/Ba3/Ke8 loop. Update the guide to distinguish allowed outer screens from each rule's inner-screen check. Verify the example under all symmetries, reversed bishop rays, stationary-king bishop moves, smaller-wall selection, retained inner-screen and escape negatives, policy/guide checks, and TypeScript. Preserve the active universal mating regression; stop at one verified fresh-clock non-mating witness and load it at cursor zero in the only tab. No commits or pushes.


R9 removal (2026-09-09): remove r9 entirely from the ordered policy and guide, its three score fields, result-wall targeting helper, and all per-candidate r9 work. Preserve r10 exactly: “Prefer fewer diagonals for Black's king, then outer bishop off target corner's edge, then White king's Euclidean proximity to the target square.” R10 remains after r8 and before r11, with squared Euclidean comparisons and the minimum-one floor, king-step target discovery/eligibility, allowed outer screens, rejected inner screens, legal Black escape checks, shared smallest-wall preferences, and no Phase 2 edge exemption unchanged. Keep the controlled-screen helper used by Phase 2 and the shared wall selector used by other rules. Remove obsolete r9-only tests while retaining meaningful wall geometry and r10 regressions. In `8/4k3/8/B7/B3K3/8/8/8 w - - 2 2`, Bb5 now reduces Black's count to nine while Kd3 retains ten, so r10 chooses Bb5 after the earlier r9 preference is removed. Verify exact rule order, absent r9 score fields and guide notes, this example and the earlier Kd5 outer-screen example under all symmetries, the existing r10 priorities and metric, policy/guide tests, and TypeScript. Keep the universal mating regression active. Stop at one verified current fresh-clock non-mating witness and load it at cursor zero in the only tab. No commits or pushes.


R24.5 outer-wall proximity (2026-09-09): add “Prefer the White king closer to the outer wall.” between r24 and r25. Measure Euclidean distance after each White candidate to the nearest actual square on the resulting outer diagonal, on either side of the wall or on the wall itself. Use squared Euclidean distances for exact ordering without square roots, and no minimum-one floor: zero is better than one. Reuse r10’s already-qualified smallest wall profiles and its complete profile comparator; among equally ranked r10 profiles, use the nearest outer wall without changing any r10 priority. No valid wall leaves r24.5 inactive with sentinel 99. Preserve allowed outer screens, rejected inner screens, legal Black-reply confinement, the shared not-inside tie preference, and removed r9/r12/r22. From `8/7k/8/8/7K/1B6/1B6/8 w - - 0 1`, Kg4 approaches outer wall a2–g8 with squared distance eight, beating Ba2’s thirteen before r25; Kg5 and Kh5 remain rejected for stalemate. Update the ordered guide and notes, verify result timing, actual diagonal squares, wall validity and symmetries, run focused policy/guide and TypeScript checks, and preserve the universal mating regression even if it fails. Stop at the first verified fresh-clock non-mating starting point, load it at cursor zero in the only tab, and do not commit or push.


R18.5 new inner wall (2026-09-09): the user sets x = 3. Insert between r18 and r19: “If it would reduce diagonals to the target corner by at least 3, build a new inner wall somewhere the Black king can neither escape nor attack.” Use the smallest valid paired wall in the starting position as the diagonal-count baseline, with the same inner-screen and legal-reply checks as r10; without one, remain inactive rather than treating sentinel 99 as an improvement. For a bishop move, examine the diagonal it newly establishes, not the diagonal it moves along. The corner may change. White’s king may not screen this new inner diagonal. Black must not already be adjacent to the moved bishop, and every legal immediate Black reply must stay strictly on the chosen corner side of the new diagonal and more than one king step from the bishop. A check on the new diagonal may force Black to one side if every reply satisfies the condition. Compare the new diagonal’s geometric corner-side count against the baseline. Give equal preference to every qualifying reduction of at least three; lower reductions receive no preference. Do not add a global frozen-White reachability search: the user’s example allows Black to cross on a later turn if White makes no progress. Preserve r10 and all preceding priorities; a new single diagonal does not itself become a paired r10 wall. Reuse result legal replies and share the starting baseline once per White scoring batch.

In `8/8/2k5/B7/4K3/8/8/5B2 w - - 6 4`, the existing a6–f1/a5–e1 pair encloses nine diagonals toward h8. Bb4 preserves that pair and establishes a3–f8 as a new inner diagonal toward a8, reducing the count to five (gain four). Black’s only legal replies are Kb7, Kc7, Kd7 and Kb6. Bf1 prevents Kb5 from attacking the bishop; Ke4 prevents escape via Kd5. Bc3 only removes two diagonals; Bc4 removes three geometrically but allows Kc5 to attack the bishop. Add the rule, explanatory notes and native diagram. Verify these cases, exact threshold, all symmetries, missing/invalid starting walls, inner screens, before/after timing and preserved earlier priorities. Keep the universal mating regression active, run focused policy/guide and TypeScript checks, then stop after the first verified current fresh-clock non-mating witness, loading it from the beginning in the only tab. Do not commit or push.


R18.5 removal and R9 wall opposition (2026-09-09): remove r18.5 completely, including its score fields, prospective-wall calculations, starting-position hypothetical Black move generation, guide text and diagram. Add r9 immediately after r8 and before r10: “When the Black king is edge adjacent to the inner wall, prefer White's king on the outer wall square in opposition to Black's king and closer to the outer wall bishop.” Evaluate each resulting position using the same qualified finite walls as r10. Choose the smallest walls with the shared not-inside tie preference before checking eligibility. Black is edge adjacent when its square shares a rank/file edge with the inner diagonal (diagonal-index difference one). Opposition candidates are actual on-board outer-wall squares exactly two orthogonal steps from Black. Within each tied wall, retain the candidates nearest its outer bishop by squared Euclidean distance, retaining equal distances. Prefer White occupying one of these squares, including when a bishop move retains that king placement; this is a binary placement preference, not an approach-distance rule. Preserve existing conditional-rule semantics: results without a qualifying target are nonapplicable. Do not add an occupied-target exclusion. Reuse qualified result wall profiles for r9 and r10 without changing r10's ordering, target score, screening or escape checks.

The user’s “15. Kg4” refers to `1BB5/8/8/6K1/8/8/6k1/8 w - - 28 15`, recovered from the preceding replay before its fifteenth White move. Inner wall b8–h2 and outer c8–h3 point toward a1. Black g2 is edge adjacent at g3/h2; g4 is the only on-board outer-wall opposition candidate. Kg4 is legal and screens only the outer bishop, which remains allowed. R9 now selects it before r10 could prefer Kh4’s proximity to h3. The later ambient position `1B6/1B6/8/8/7K/4k3/8/8 w - - 32 17` has opposition candidates e5/g3; e5 is nearer outer Bb8, but cannot be reached immediately from Kh4, so it grants no approach bonus. Add a native opposition diagram, update rule order and definitions, and verify board symmetries, candidate ranking/ties, result timing, smaller-wall selection, screening/escape negatives and prior priorities. Keep the universal mating regression active, validate policy/guide and TypeScript checks, then stop at one verified fresh-clock non-mating starting point and load it from the beginning in the only tab. No commits or pushes.


R9 inner-bishop attack exception (2026-09-09): append “unless the inner wall bishop could be attacked.” to the r9 rule. For each candidate’s resulting smallest qualified wall, remove its r9 eligibility if Black already attacks that wall’s inner bishop or any legal immediate Black reply would attack it. A king attacks from an adjacent square even if the bishop is defended; this is not merely an immediate-capture test. Use all legal reply destinations already generated for that candidate, with king-step distance at most one, and test only the inner bishop. Apply the exception after the existing smallest-wall/not-inside selection, so an unsafe small wall cannot be replaced by a larger wall, but retain any equally preferred safe wall. Preserve r10 wall validity and scores, opposition geometry, nearest outer-bishop candidate selection, occupied targets, result timing and conditional applicability.

From `8/2B5/8/3K2k1/8/7B/8/8 w - - 18 10`, Ke5 would stand on the outer opposition square but permits Kh4 to attack inner Bh3. R9 is therefore inactive and r10 now selects Ke4. The earlier `1BB5/8/8/6K1/8/8/6k1/8 w - - 28 15` still selects Kg4 because inner Bb8 cannot be attacked. Update the rule, note and existing diagram caption. Verify direct/current and next-reply attacks, defended bishops, outer-only threats, bishop relocation and board symmetries; run policy/guide and TypeScript checks. Keep the universal mating regression active regardless of outcome. Stop at the first verified fresh-clock non-mating witness and load its replay at cursor zero in the only tab. No commits or pushes.

R9 inner-bishop distance exception (2026-09-09): replace the previous attackability exception with “unless Black's king is closer to the inner bishop than White's king.” Compare both kings to that wall's inner bishop using squared Euclidean distances in the resulting position. Disable that wall's r9 eligibility only when Black is strictly closer; equal distances remain eligible. This supersedes the current-adjacency and next-legal-reply attack checks. Remove the unused reply-list parameter from the r9 helper, without changing r10's existing reply-based wall qualification. Preserve shared smallest-wall/not-inside selection before the exception, result-time bishop identity and king positions, outer-bishop proximity, tied opposition targets, conditional applicability, and rule order. Update the guide and existing diagram caption; validate strict inequality, equality, Euclidean versus king-step distance, resulting positions and symmetries. Keep the universal mating regression active, stop at one verified fresh-clock non-mating witness, and load its full replay at cursor zero in the sole tab. No commits or pushes.

R9 opposition nearest both bishops (2026-09-09): supersede both previous exceptions with “When the Black king is edge adjacent to the inner wall, prefer White's king on the outer wall square in opposition to Black's king and closer to both bishops.” The user clarified that this compares the candidate opposition squares with each other, not White's distance against Black's. Within each selected resulting wall, compute the minimum squared Euclidean distance from the candidate opposition squares to each bishop independently. Retain only squares attaining both minima, including exact ties. If the bishops favor different squares, that wall has no r9 target. A sole on-board opposition square qualifies without a king-versus-king distance or attackability test. Preserve shared smallest-wall/not-inside selection, result timing, occupied targets, conditional applicability, binary placement preference, wall qualification and rule order. Update guide and diagram; test agreeing/disagreeing bishop preferences, one or both bishop ties, one candidate, result-time bishop moves, and board symmetries. Preserve the active universal mating regression and stop at one verified current fresh-clock non-mating witness with counts and a full replay at cursor zero in the only tab. No commits or pushes.

R19 king proximity condition (2026-09-09): replace its help with “If the white King is on or adjacent to the outer diagonal, prefer the outer bishop at least 3 steps away from Black's king.” Evaluate each candidate's resulting position. Keep the existing r19 bishop-wall geometry and shared smallest-count/not-inside tie selection, then retain only walls whose actual on-board outer diagonal is at most one king step from White's king. Diagonal adjacency counts, and geometric adjacency does not require a legal king move. Compute this using the king's at-most-nine on-board neighborhood squares, avoiding extra move generation and false proximity to diagonal extensions. Add explicit ruleR19Applies and use existing conditional-rule semantics so ineligible candidates are not rewarded or rejected by r19. Among eligible tied walls retain the existing at-least-three king-step distance requirement for that same wall's outer bishop from Black; do not take proximity from one wall and bishop distance from another. Missing walls or kings leaves r19 inactive. Preserve r9 and all other rule priorities, r10 qualification and result geometry. Update help/notes, test on/adjacent/far including diagonal adjacency, threshold distances, result timing and symmetries, and keep the universal mating regression active. Stop after one current fresh-clock non-mating witness with rule-filter counts and a full replay loaded at cursor zero in the sole tab. No commits or pushes.

Unscreened target squares (2026-09-09): update the target definition to the user's exact text, “Target squares are the unscreened outer-wall squares closest to Black's king by king-step distance. All equally closest squares are candidates, including bishop-occupied squares. White's king must be outside the wall, or on the outer wall and no farther from the target than Black's king by king-step distance.” In each selected resulting r10 wall, first remove outer-diagonal squares strictly beyond White's king away from its outer bishop, whenever White is on that diagonal. The king's square itself is unscreened; even king-controlled squares beyond it are screened. Then minimize Black's king-step distance among the remaining squares and retain all ties, including the bishop square. Apply unchanged White eligibility after closest-candidate selection. This does not invalidate an outer-screened wall or change its diagonal count, and does not change r9 opposition targets or r19 adjacency. R24.5 still measures distance to the full actual outer wall, reusing the resulting r10 profile ranking. Keep r10's squared Euclidean White-target score with its minimum-one floor and all inner-screen/Black-reply checks. Update guide/diagram captions and target regressions, test both ray directions and board symmetries, tie filtering, occupied/king-square targets, farther unscreened fallback, and no-screen behavior. Preserve the active universal mating regression, then stop at the first current fresh-clock non-mating witness and save counts and a full replay at cursor zero in the only tab. No commits or pushes.

Unscreened-candidate ordering correction (2026-09-09): the user clarified that after 8.Kf5 there must be no target squares because the closest candidates are screened. Supersede the previous farther-unscreened fallback: compute minimum Black king-step distance over the entire outer wall, retain all equally closest candidates, then remove screened candidates and apply unchanged White eligibility. If all candidates are screened, return no targets and proximity score 99; do not promote a farther unscreened square or White's own square. A king-occupied or bishop-occupied closest candidate is still permitted when otherwise eligible, and partially screened ties retain their unscreened members. Keep wall validity/counts and r24.5's full-wall distance unchanged. In `8/8/6B1/8/6K1/8/8/2B1k3 w - - 14 8`, Kf5 keeps five diagonals but excludes closest d3 and has no targets. Update affected tests and guide notes, retain the universal mating test, then stop at one verified current failure and load it at cursor zero in the only tab. No commits or pushes.


## Inside-wall target eligibility (2026-09-09)

User explicitly allows target squares when White’s king is inside the wall. Remove that exclusion; retain closest-candidates-first, screening exclusions, bishop occupancy, and the on-outer-wall no-farther king-step requirement. R10 still scores White’s Euclidean distance with a floor of one. Verify 8.Kf3 from `8/8/6B1/8/6K1/8/8/2B1k3 w - - 14 8`, update affected assertions and notes, and stop at one verified current non-mating start. No commit or push.


## Inner screens depend on Black’s legal replies (2026-09-09)

User allows inner-wall screening as long as Black cannot walk onto a screened inner-wall square. Evaluate immediate legal Black replies after the White move. Remove blanket screen invalidation from r10 and the stronger every-hidden-square-controlled Phase 2 condition; retain checks rejecting legal entry onto the wall and escape beyond it, and retain other bishop-ray blockers. Existing generated legal replies already decide inner-wall entry, avoiding a redundant ray scan. Target screening rules remain unchanged. Verify Kf6 from `8/8/7k/5K2/8/8/BB6/8 w - - 0 1`, guarded and distant hidden squares, and reachable hidden-square counterexamples across symmetries. Stop at one verified non-mating start; no commit or push.


## Accomplished r9 outranks exemption (2026-09-09)

User explicitly requests an accomplished r9 outweigh an exempted r9. Compare the existing r9 penalty across all surviving moves: occupying a qualifying opposition square scores 0; an exempt move and an applicable but unaccomplished move both score 1. Remove only r9’s per-candidate applicability gate; keep the geometry diagnostic and every other rule’s applicability unchanged. If no move accomplishes r9, r9 does not filter. Verify 2.Ke4 beats Bc1 from `8/8/6BB/4K3/8/8/4k3/8 w - - 2 2` across all symmetries. Update notes and relevant regressions, then stop at one current non-mating witness with rule-filter counts and a full replay loaded at cursor zero in the sole tab. No commit or push.


## Specific r10 starting-position exemption (2026-09-09)

User requests a note and diagram plus an r10 exemption for `8/8/6B1/8/8/4K3/1B6/4k3 w - - 2 2`. Match White king e3, Black king e1 and the outer bishop b2 exactly, allowing the other bishop anywhere on inner b1–h7, including endpoints; support board rotations/reflections and ignore move counters. Decide exemption once from the starting position and skip all r10 subpriorities for every candidate that turn. Preserve calculated resulting wall scores for other rules. Add a diagram highlighting all seven inner-diagonal squares, test the exact fixture, every bishop location, counter independence, symmetry, near misses, and starting-versus-result timing. Stop at the first verified current non-mating start. No commit or push.


## Replace the specific r10 exemption with r9.5 (2026-09-09)

Remove the exact-piece r10 exemption, its score/context flags and its note/diagram. Restore r10 for every position. Insert r9.5 between r9 and r10 with the user's wording: “When the black king is on the edge and the white king in non edge opposition and adjacent to 2 inner wall squares, place the king on the outer wall.” Evaluate the trigger once from the starting position. Black must have edge distance zero; White must have positive edge distance; the kings must be two squares apart on a shared rank or file. Use the smallest starting reply-qualified bishop walls and existing not-inside tie preference. White must have exactly two king-step-adjacent squares on the chosen inner diagonal. Give a binary preference to a legal king move onto that same starting outer diagonal; no approach-distance bonus, fixed bishop-square pattern, or global king restriction. Keep targets fixed across candidate moves and ignore move counters. Replace the old exemption diagram with the f6/h6, c8/g3 example: inner c8–h3, adjacent squares e6/f5, outer b8–h2, arrow f6–e5. Validate all eight symmetries, variable bishops, all trigger near misses, starting/result timing, and r10 reactivation. Keep the active universal mating regression. Stop at one verified current non-mating starting point, with a replay at cursor zero and rule-filter counts. No commit or push.

Priority issue found during implementation: the example Ke5 is eliminated by r6's Phase 2 diagonal preference before r9.5. The user has been asked whether r9.5 should override r6 in this geometry or retain that earlier priority; no override is implemented pending the answer.


## Move r9.5 to r2 (2026-09-09)

User resolves the earlier precedence question by moving r9.5 to r2. Rename the rule, internal score/context geometry fields, note, diagram, and focused tests, and place it after the universal mate/bishop-safety/no-stalemate priorities and before r3. Keep its trigger, starting-position timing, wall qualification, targets and binary placement preference unchanged. No r6 exemption is needed: r2 now runs first. Confirm Ke5 from `2B5/8/5K1k/8/8/6B1/8/8 w - - 0 1` is the full-policy recommendation, in all eight symmetries, with Bh4 removed by r2. Keep r10 universally active and the universal mating regression intact. Stop after one verified current non-mating starting point, save counts, and load its replay at cursor zero in the sole tab. No commit or push.


## Undo promotion to r2 (2026-09-09)

User requests undoing the preceding change. Restore the rule name r9.5 and its position between r9 and r10, along with the prior internal names, diagram and tests. Keep the r10 exemption removed. R6 again precedes r9.5; no priority override is pending or authorized. Preserve all earlier policy work. No commit or push.


### Promote r8 to r1, then r9.5 to r2 (2026-09-09)

The user explicitly requests this ordering: rename r8 to r1 and place it immediately after the universal mate, bishops-safe, and no-stalemate filters; rename r9.5 to r2 and place it immediately after r1. Preserve each rule’s scoring and applicability. All other numbered rules retain their relative order. Update code, labels, diagram references, and focused tests together. The removed r10 exemption remains removed. Validate the ordering and example behavior, then stop the search after one independently replayed non-terminating starting position, recording rule filtering counts and a clickable replay. Do not commit or push.


### Restrict r2 to Phase 1 (2026-09-09)

The user adds “Phase 1:” to r2. Gate its existing starting-position geometry on the existing phase definition: r2 is exempt when the starting position matches an established r4 mating pattern (Phase 2). Reuse the already-computed r4 matches in the scoring context rather than recomputing phase for every candidate. Preserve the ordering, scoring, wall geometry, and Phase 1 behavior. Update the guide and notes. Verify the Kf5 loop and symmetry/counter invariance, then stop at one verified non-terminating starting position with filtering counts. No commit or push.


### R9 requires strictly closer to both bishops (2026-09-09)

The user clarifies that Bf4+ from `2B5/8/8/4K1k1/8/6B1/8/8 w - - 0 1` must not receive r9 credit: outer-wall opposition candidates e5 and g3 are equally far from Bf4. Compare resulting candidate squares with each other, requiring strictly smaller Euclidean distance to each bishop than every other candidate. Ties for either bishop leave no target for that wall, even if the other bishop prefers one candidate. Preserve the sole-candidate case, qualified/smallest wall selection, resulting-position timing, and accomplished-over-exempt priority. Update notes and tie regressions; stop at one verified non-terminating starting point. No commit or push.


### Move r2 to r10.5 (2026-09-09)

Rename r2 to r10.5 and place it immediately after r10, before r11. Preserve its starting-position Phase 1 gate and all wall/opposition geometry. Keep r1 and every other rule in their current relative order; retain strict r9 bishop-distance comparisons. Update score fields, guide labels, notes, diagram references, and ordering tests. Earlier rules now decide before this outer-wall step preference. Stop at the first verified non-mating starting position; record rule filtering counts and provide a replay. No commit or push.


### Replace r10.5 with r9.5 forcing corner progress (2026-09-09)

Remove r10.5 and its outer-wall step geometry/Phase 1 restriction. Insert r9.5 between r9 and r10 with the user's wording. Trigger on starting direct opposition (two squares apart on the same file/rank), Black on an edge, White off all edges. Use the existing r6 starting target-corner selection (smallest geometric walls with normal White-inside tie handling; nearest Euclidean corners if no wall), shared once per scoring context. Keep those corners fixed across candidates. Prefer moves with at least one legal Black reply and every legal reply strictly nearer to one common target corner by king-step distance. No post-move wall or White-king placement requirement. Bg4 from the supplied starting FEN qualifies because only Kh7 is legal, closer to h8; Bh4 fails because Kh5 is also legal. R6 currently removes Bg4 before r9.5; a specific r6 exception has been asked about and is pending, so retain r6 until clarified. Update guide diagram and tests. No commit/push; stop after one verified non-mating start.


### Move the forcing rule r9.5 to r2 (2026-09-09)

The user resolves the pending priority question by explicitly promoting r9.5 to r2. Rename the rule and move it immediately after r1, before r3 and therefore before r6. Preserve its forcing score, starting target-corner selection, and applicability in either phase. No r6 exception is needed or pending; r6 itself is unchanged. Update all live labels, score fields, diagram references, and ordering tests. Assert Bg4 is selected by the full policy in all eight symmetries and at three move-counter settings. Search until one non-mating start, recording rule counts. No commit or push.


### Restore forcing rule to r9.5 (2026-09-09)

The user explicitly undoes the promotion to r2. Restore the forcing rule’s name r9.5 and position between r9 and r10, including internal names, guide, diagram, and tests. Preserve its forcing behavior and applicability in either phase. R6 again runs first, with no exception added. This instruction resolves the priority choice by restoring the previous order; no approval or clarification is pending. Verify the production fingerprint against the prior r9.5 policy and reuse its verified non-mating witness if identical. No commit or push.


### Use the inner bishop and move the forcing rule to r5.5 (2026-09-09)

The user restricts forcing progress to the inner bishop, then promotes r9.5 to r5.5 between r5 and r6. Identify inner bishops in the starting smallest geometric bishop walls with existing tied-wall White-inside preference. Keep each inner bishop paired with its own starting target corner; success requires that bishop to move and every legal Black reply to get strictly nearer to that corner by king-step distance. At least one reply is required. No starting wall means no inner bishop and no applicable rule; remove the former no-wall nearest-corner fallback. Outer-bishop and king moves receive no credit. Keep starting edge/opposition conditions and applicability in either phase. Update notes, diagram, and tests, including Bg4 full-policy preference and rejection of the outer-bishop Bd2 loop move. No r6 exception needed. Stop after one verified non-mating starting position; no commit or push.


### R10 final tie-breaker: bishop mobility along each wall (2026-09-09)

Append the user’s requested preference after target-square Euclidean proximity. Count bishops with no legal move along their own diagonal in the candidate’s selected resulting wall: prefer zero immobile bishops, then one, then two. Additional legal destinations do not earn extra credit. Measure immediately after White moves, before any Black reply, with ordinary chess legality rather than capture safety. Preserve earlier priorities and compare complete scores within each wall; r24.5 continues to reuse the selected r10 wall profiles. No-wall candidates share the default of two. In this four-piece endgame, bishop movement cannot expose a sliding check; testing the adjacent square on each wall ray for emptiness is equivalent to existence of a legal along-wall bishop move and avoids generating another full move list for every candidate. Validate against chess.js move generation, symmetry, blocker cases, priority precedence, and the loaded loop. Stop after one non-mating starting point. No commit or push.


## 2026-09-09: Split enclosure count into r8 before opposition

The requested order is r8 (fewer Black diagonals), r9 (existing accomplished outer-wall opposition), then r10 (outer bishop off the target corner edges, White king Euclidean target proximity, and finally bishop wall mobility). Preserve wall qualification, smallest-wall selection, target eligibility, distance flooring, and all exceptions. The shared result-wall score remains computed once; r8 consumes its diagonal count and r10 consumes the other three fields. No rule is allowed to choose a larger wall merely to improve its later preferences. The internal diagonal-count field retains its existing name for diagnostic compatibility.

Implementation and validation: insert r8 after r6 and before r9, remove diagonal count from r10's comparator, update guide notes and ordering assertions, test the Ke7/Bh2 precedence across board symmetries, and stop verification after the first independently replayed non-terminating witness. Do not commit or push.

Result: the original start now always mates within 39 plies. Search stopped at the first verified loop, `8/8/8/6B1/8/5K1k/2B5/8 w - - 0 1`: Bf6 Kh2 Bg5 Kh3. Evidence and scoped rule counts are in `_codex_output/two-bishops/r8-split/report.md` under the workspace repositories directory.


## 2026-09-09: Remove r5.5

Remove the inner-bishop corner-forcing preference, its dedicated starting-wall calculation and score fields, and its guide note and diagram. R5 is followed directly by r6. All remaining rules retain their order, scores, and exceptions. Update the rule and presentation assertions, replay the current loop under the new policy, and stop after one verified non-terminating starting position. No commit or push.

Validation outcome: the previous loop now always mates within 17 plies, beginning Bd1. Stopped at the new verified loop `8/8/6B1/8/8/4K3/8/2Bk4 w - - 0 1`: Bb2 Ke1 Bc1 Kd1. See workspace `_codex_output/two-bishops/remove-r5-5/report.md` for evidence and scoped counts.


## 2026-09-09: Restore r5.5

Restore the just-removed r5.5 exactly between r5 and r6: with starting edge opposition, use the selected starting inner bishop to force every legal Black reply closer to its paired target corner. Restore its score fields, starting-wall context, guide note and diagram. Preserve the r8/r9/r10 split. Validate the restored rule and replay one non-terminating witness; no commit or push.


## 2026-09-09: R5.5 must retain the inner bishop

The moved starting inner bishop must remain the inner bishop after White moves, before Black replies. Use selected smallest valid resulting walls, with the usual White-not-inside tie preference, and require the same starting target corner. Keep the existing every-legal-reply corner progress requirement. Switching to the outer wall or destroying the wall gets no r5.5 credit. In the supplied Bf6 loop, Bf6 must score a failure despite forcing Kh2. Update notes, check all symmetries and the established Bg4 success case, then stop at one verified non-terminating starting position. No commit or push.

Result: Bf6 no longer receives r5.5 credit; Bd1 is selected and the old start always mates within 17 plies. Stopped at `4BB2/4K3/8/2k5/8/8/8/8 w - - 0 1`, with loop Bg6 Kb6 Be8 Kc5. Evidence and scoped counts are in workspace `_codex_output/two-bishops/r5-5-remains-inner/report.md`.


### R6 unscreened Phase 2 diagonals (2026-09-09)

Require both resulting Phase 2 bishop diagonals to have uninterrupted bishop rays to their endpoints before r6 grants diagonal credit. White’s king may not screen either ray, regardless of hidden-square control, distance from the edge, or Black’s ability to reach the hidden squares. An endpoint king screens no square beyond it. Preserve actual Black-reply confinement and all subsequent r6 priorities. Update the rule text and shared notes to distinguish this stricter r6 requirement from unchanged r8/r10 walls. After 16.Bh4 from `2B5/8/4K2k/8/8/6B1/8/8 w - - 30 16`, Ke6 screens Bc8 on c8–h3, so r6 must give no Phase 2 diagonal credit. Validate outer/inner screens and endpoints across symmetries, run focused checks, then stop at one verified non-terminating starting point with rule-filter counts and a replay at cursor zero in the only tab. No commit or push.


### R6 king behind the bishop exception (2026-09-10)

For each resulting Phase 2 bishop ray, ignore White’s king as a screen only when Black’s Euclidean distance to that bishop is strictly less than Black’s distance to White’s king. Compare squared distances after White moves; equality does not qualify. Apply separately to each bishop, preserving all other blockers and actual Black-reply confinement. This supersedes the blanket r6 screen rejection, while r8/r10 qualification and target screening remain unchanged. In `8/8/8/8/8/3KB2k/4B3/8 w - - 2 2`, Kd2 is behind Be3 relative to Black h3: distances 3 versus sqrt(17), so the ray qualifies. Test the example, equality, reversed rays, and symmetries. Preserve the universal mating regression, stop at one independently replayed non-terminating start, record rule-filter counts, and load it at cursor zero in the sole tab. No commit or push.


### R6 equidistant screen exception (2026-09-10)

Include equality in the preceding r6 exception. White’s king does not count as a screen for a bishop when Black is no farther from that bishop than from White’s king, measured by squared Euclidean distance after White moves and before Black replies. Preserve all other blockers, confinement checks, r8/r10 and target screening. From `8/8/8/8/8/4B3/3K4/3B1k2 w - - 2 2`, Be2+ leaves Black f1 equidistant (sqrt(5)) from Be3 and Kd2, so the outer ray qualifies immediately. Update notes and equality regressions across symmetries, keep the universal mating regression, and stop at one verified non-terminating start with rule counts and a replay in the only tab at cursor zero. No commit or push.


### R24 accepts non-confining bishop walls (2026-09-10)

Use resulting geometric adjacent-diagonal walls for r24’s no-wall gate, before checking Black’s legal replies. A bishop wall may disable r24 even when Black stands on a wall, can enter a wall, or can cross beyond it. Preserve the existing geometric length/side bounds, the starting king moat, and the stricter reply-qualified walls for r8/r9/r10/r24.5. Reuse the geometric wall list already needed for those rules, avoiding new move generation. In `4BB2/4K3/8/2k5/8/8/8/8 w - - 0 1`, Ke6+ retains a3–f8/a4–e8 as a geometric wall even though Black can escape via Kb6; r24 must not reject it. This supersedes the earlier use of the r10 count sentinel for r24. Update notes and tests across symmetries, preserve universal mating checks, and stop at one verified non-terminating start with rule-filter counts and a replay at cursor zero in the only tab. No commit or push.


### R24 wall before moat (2026-09-10)

Replace r24 with “Prefer a bishop wall, otherwise prefer a bishop inside a king moat.” Compare all candidates with penalty 0 for a resulting geometric wall, 1 for no wall but a bishop in the fixed starting moat, and 2 for neither. Remove its per-candidate applicability gate; the existing applicability diagnostic now indicates a resulting wall or a starting moat but never suppresses comparison. A missing moat cannot exempt a no-wall move from losing to a wall. Preserve non-confining-wall acceptance, existing geometric bounds, result timing, starting moat geometry and no bonus for multiple moat bishops. Update notes/tests, check Be6+ from the earlier a2/h2 bishop loop, preserve universal mating checks, then stop at one independently verified non-terminating start with rule counts and sole-tab replay at cursor zero. No commit or push.


### R7 prevents outer-wall entry and inner-bishop threats (2026-09-10)

Insert r7 between r6 and r8: “Prefer not allowing Black onto the outer diagonal or attacking the inner bishop.” Evaluate after White moves, using existing legal Black replies and smallest reply-qualified walls with shared tied-wall preference. Give penalty 0 if at least one selected wall prevents every reply from entering its outer diagonal or coming within one king step of its inner bishop; otherwise 1. A defended bishop can still be attacked. Both requirements must hold for the same wall, without choosing a larger wall to evade a threat. No valid wall receives no credit. Compare all candidates without an applicability gate; do not double-penalize both threats. Preserve all preceding priorities and later wall scoring. Update guide/order checks, test each threat and all board symmetries, then stop after one independently verified non-mating start with rule-filter counts and a sole-tab replay at cursor zero. No commit or push.


### R7 conditional inner-bishop attack check (2026-09-10)

Narrow only the inner-bishop attack condition: apply it when the resulting inner bishop is adjacent by one king step to an outer-diagonal square screened by White’s king. A screened square lies beyond White’s king away from the outer bishop on that diagonal; the king’s own square does not count. Always retain the outer-diagonal entry check. Preserve smallest reply-qualified walls, tied-wall preference, all legal replies, and the single combined penalty. Bf8 after Ba2 Kd7 from `4k3/6B1/5K2/8/8/1B6/8/8 w - - 0 1` must pass r7 because Kf6 does not screen a2–g8, even though Ke8 attacks Bf8. Preserve Bh4 rejection with Ke6 screening c8–h3. Validate symmetries and unchanged universal mating regressions, then stop at one independently verified non-mating start with rule-filter counts and a sole-tab replay at cursor zero. No commit or push.


### R10 king-step target proximity (2026-09-10)

Replace only r10’s Euclidean target-distance priority with king-step (Chebyshev) proximity, evaluated after White moves. Keep the minimum score of one: occupying a target, orthogonal adjacency and diagonal adjacency tie. Preserve missing-target score 99, target selection/eligibility/screening, outer-bishop edge priority before proximity, bishop wall mobility after proximity, and all other rules’ distance metrics. R24.5 remains Euclidean. Update guide, score-unit assertions and metric-specific tests; preserve unrelated desired-move and universal-mating regressions. Stop at the first independently verified non-mating start, record scoped rule-filter counts, and load the verified replay at cursor zero in the sole tab. No commit or push.


### R24.5 approaches the diagonal beyond the outer wall (2026-09-10)

Change r24.5’s destination from the outer wall to the parallel diagonal one index farther away from the target corner. Retain squared Euclidean distance to the closest on-board square, evaluated after White moves. Retain zero on arrival, no r10 distance floor, existing r8/r10-selected wall profiles and minimum distance among exactly tied profiles. A valid target does not exempt the rule. No valid wall or no on-board beyond diagonal yields inactive score 99. For outer a2–g8 toward h1, beyond means a3–f8, consistent with the earlier definition. Update guide and metric-specific tests; preserve unrelated historical desired-move regressions. Stop at one independently verified non-mating start, record rule-filter counts and load the replay at cursor zero in the sole tab. No commit or push.


### R9 outer bishop off the target-corner edges (2026-09-10)

Add a resulting-position prerequisite to each r9 wall: its outer bishop must not lie on either edge meeting at that wall’s target corner. Apply after selecting the existing smallest reply-qualified walls and their usual ties; do not substitute a larger wall. An on-edge outer bishop gives no r9 target or accomplishment credit, even if White’s king occupies the geometric opposition square. Retain strict Euclidean comparison of opposition candidates to both bishops, single-candidate behavior, accomplished-over-exempt scoring, and all other rule priorities. In `8/8/2k5/2B5/3K4/8/B7/8 w - - 2 2`, Kc4 cannot accomplish r9 because Ba2 is on the a8 corner’s edge; Bb3 moves that outer bishop off the edge and restores r9’s c4 target. Update notes/tests, preserve unrelated desired-move regressions, then stop at one independently verified non-mating start with scoped rule counts and a sole-tab replay at cursor zero. No commit or push.


### Target squares exclude bishop occupancy (2026-09-10)

After selecting all closest outer-wall candidates by Black’s king-step distance, exclude each candidate occupied by a bishop in the resulting position. Retain equally close unoccupied candidates subject to existing screening and White-king eligibility checks. Do not substitute farther candidates and do not invalidate the wall or the other candidates because one is occupied. White’s king may still occupy a target. Keep r10’s king-step score and minimum-one floor, r24.5’s beyond-diagonal metric, and all other rules unchanged. In `1k6/8/B7/8/1K6/8/8/6B1 w - - 34 18`, Ka5 previously approached occupied a6; it must now have no target. Kb5 tied proximity but lost the bishop-mobility tie because it screened Ba6’s only wall move. Update notes and occupancy tests across symmetries, preserve unrelated historical desired-move regressions, then stop at one independently verified non-mating start with rule-filter counts and sole-tab replay at cursor zero. No commit or push.


### R8 counts the side Black chooses after a crossing reply (2026-09-10)

Do not automatically invalidate a wall when a legal Black reply crosses beyond both diagonals. For each reply, count the diagonals on its resulting side and use the maximum across legal replies; never sum disjoint alternative sides. A legal landing on the original inner diagonal remains invalid, and landing on the outer diagonal retains the original-side count plus one. Preserve geometry and wall-selection priorities. This qualification is shared by the existing wall-dependent rules; their orientation is still evaluated after White’s move, and is naturally recalculated after the actual Black reply on the next turn. From `8/8/8/K7/2k5/8/8/3BB3 w - - 10 6`, Be2+ has inner a6–f1 and outer a5–e1: Kc5/Kd5/Kd4 leave nine diagonals toward h8, while Kb3 leaves four toward a1. The worst reply count is nine, so r8 must tie Be2+ with Bd2 instead of rejecting it using the fallback ten-diagonal wall. Update guide and focused symmetry/reachability tests, preserve historical desired-move expectations, and stop at one independently verified non-mating start with rule counts and a sole-tab replay at cursor zero. No commit or push.


### R8 crossing correction: strictly smaller escape area

A legal reply crossing both walls preserves the wall only when the opposite area contains strictly fewer diagonals than Black's original side after White's move. Equal or larger opposite areas invalidate that wall; keep the existing rejection of a reachable inner-wall square. This qualifies the preceding resulting-side counting update.

In `8/B7/2K5/4k3/8/8/6B1/8 w - - 0 1`, Bb8+ allows Kd4 from the original six diagonals into seven, so r8 rejects its wall. Preserve Be2+ in `8/8/8/K7/2k5/8/8/3BB3 w - - 10 6`: Kb3 has only four rather than nine diagonals. Verify both positions and their symmetries, the inner-wall reachability regressions, guide text, and TypeScript. Search until the first verified non-terminating witness, then stop. No commit or push.


### Completed exhaustive audit — 2026-09-10

All 312,286 symmetry-distinct app-eligible opposite-colored KBBK White-to-move starts mate within 95 plies from a fresh halfmove clock, including every tied-best White move and every legal Black reply. The complete graph contains 312,426 White positions, 312,998 preferred White choices and 1,314,632 Black replies. No loops, terminal failures or fifty-move-limit failures were found. Independent Kosaraju SCC and longest-path analysis agrees. Census runtime was 819.188 seconds with six workers; the unchanged, fingerprint-validated complete root list was reused, with every policy expansion recalculated.

Twenty verifier checks, 12,616 sampled/adversarial symmetry comparisons and 200 clock checks pass. Focused r8 tests, presentation checks and TypeScript pass. The broad rule suite remains at 181 passed and 32 pre-existing failures; those expectations were not rewritten to obtain a clean result. The preserved universal former-stalemate mating regression passes. This certificate is scoped to app-eligible starts with no supplied repetition history, not arbitrary FENs or an already-consumed halfmove budget.

Policy fingerprint: `ef0b10fe235c94e8340065a39e404b79d5177f9ee5d222d0820e7ff0dc86a0af`. Engine fingerprint: `12d2647dc3b8407c37ff489627fa99fa5fd922568b6da30e9853a58c388922eb`. Verifier implementation: `e6d5354c079e77646375b903944b4feb3acc4c9e0950f5f3bd8e1b7a7df4277b`.


### R4 wording, r6 geometry priority, and r19 removal

R4 now reads exactly “Execute the mating pattern.” Its applicability and mating-pattern behavior are unchanged. R6's final Euclidean proximity compares White's king after the candidate move with its Phase 2 king square in the selected matching template—the same destination as its preceding shortest-path score. Name that reference explicitly in the guide without changing the calculation.

Move the flank step from r11 to r6.2 and the choke move from r18 to r6.4, immediately after r6 and before r7, retaining each rule's geometry and applicability. Rename the choke diagram and diagnostic score fields consistently. Remove r19 entirely, including its evaluation, applicability fields and explanatory note. Update obsolete r19 tests and rule-order/guide tests; preserve unrelated expectations and the universal former-stalemate regression. The preceding exhaustive certificate covers the prior policy and does not certify this change. Run focused checks and stop the follow-up search at its first verified counterexample. No new commit or push unless requested.


### Restore r19 after r10

Restore r19's previous geometric-wall eligibility and three-king-step outer-bishop preference, its score fields and explanatory note, immediately after r10 and before r24. Keep r4's simplified wording, r6's clarified Euclidean reference, and the flank/choke rules at r6.2/r6.4. Restore r19 regression coverage and the force-corner note's original attachment index. Search for a single counterexample under this combined policy; do not assume the older exhaustive certificate still applies. No commit or push requested.


### Notes rewrite — 2026-09-10

Rewrote the user-facing notes for accuracy, brevity, and clarity. Notes fall from 1,233 to 452 words; diagram captions from 217 to 108. Grouped shared wall and screen definitions, retained consequential rule exceptions, removed repeated priorities and obsolete comparison commentary, and corrected the target-square caption to exclude bishop-occupied targets. The r5.5 diagram now follows its relocated note. Rule implementation, order, and scoring are unchanged. Targeted guide/policy-description checks and verifier TypeScript checks pass. No new termination audit is needed for this text-only change.


### Notes editorial correction — 2026-09-10

Reviewed every note and caption against the user's request to omit reasonable assumptions. Removed inferred consequences, obvious definitions, repeated rule instructions, and redundant diagram descriptions. Notes now contain 272 words, down from 452 in the first rewrite. Kept only necessary definitions, non-obvious conditions, and measurement conventions. Rule implementation is unchanged; targeted guide checks pass.


### Assumed chess knowledge — 2026-09-10

Removed the screen definition, defended-bishop attack explanation, and reachable-diagonal counting explanation. Shortened r24.5 to its distance metric. Notes should document policy-specific choices, not explain familiar chess terms or consequences already implied by the rules. No rule behavior changed.


### Final notes scope — 2026-09-10

Retain only the target-square explanation and all six diagrams with their captions. Remove every other note. Detach the r5.5 diagram from its deleted note so it remains visible. Rule behavior is unchanged.


### Diagram simplification — 2026-09-10

Removed diagonal highlights from the target-square and r9 diagrams. The target diagram retains f4 emphasis and uses only the d2–e1 bishop arrow. Mirrored r9 horizontally, including its pieces, move arrow, and caption. Removed the r10 and r6.4 diagrams. Removed all r5.5 square highlights and its Black king arrow, retaining c8–g4. Four diagrams remain; the target-square explanation is unchanged. Targeted guide tests and TypeScript pass.


### Final diagram orientation and validation

The target-square diagram is shifted one rank up and two files left: White Kc5, Black Ke4, bishops b3/b2, target d5, and arrow b3–c2. The r5.5 diagram is reflected vertically, with bishop arrow c1–g5. The r9 diagram is reflected horizontally and then across a8–h1, with White Kd7, Black Kg7, bishops a2/a3, and arrow d7–e7.

After r19 was restored alongside r6.2/r6.4, all 1,003 sampled starting positions terminated within 95 plies; this is not a new exhaustive certificate. Final guide checks, r19 checks, TypeScript and whitespace checks pass. The checked r6.4/r8 suites reproduce two failures already recorded in the prior audit. The user requested committing and pushing the accumulated changes.


### Diagram layout — 2026-09-10

All guide diagrams render their board before the title and caption. The four two-bishop diagrams share one grid beneath the target explanation. Full-board columns flex from 14rem so four fit at the desktop guide width; the existing narrow-screen stack remains. Default board layouts receive the same full-board grid sizing as explicit 8×8 layouts. Cards align content at the top. Eleven targeted guide/layout checks, TypeScript, and diff whitespace checks pass.
