# Mate Landing Value Proposition Design

## Goal

Replace the brief Mate landing explanation with a persuasive 50–100-word
description grounded in the tool's actual learning experience.

## Product evidence

The copy may promise only behavior present in the app:

- recommended moves come from visible, ordered, human-readable rules;
- Black selects stubborn replies;
- reason hints and the move log expose the decision process;
- Undo, Redo, and Play Best support exploration;
- Standard generates varied positions; and
- Training Wheels draws from curated patterns and transforms them.

These features work together to teach a reusable mating plan rather than an
engine sequence or a line to memorize.

## Copy

Replace the current introductory paragraph with this 83-word paragraph:

> Most mating trainers rely on engine moves or ask you to memorize a fixed
> line. Lotta Endgames teaches a repeatable plan instead. Every recommended
> move follows visible, human-readable priorities, while Black chooses stubborn
> replies so you learn the pattern—not a script. Reason hints show what to look
> for, the move log explains each decision, and Undo, Redo, and Play Best let
> you explore alternatives. Standard positions build adaptability; Training
> Wheels isolates useful patterns. The goal is confidence you can carry into
> real games.

Keep the paragraph above `Choose a mating set` and retain the existing
selection prompt below the heading.

## Verification

- Assert the exact paragraph and its placement before the chooser heading.
- Assert the paragraph contains between 50 and 100 whitespace-delimited words.
- Preserve landing catalog and no-drill behavior.
- Run the focused presentation test, lint, build, and a diff check.
